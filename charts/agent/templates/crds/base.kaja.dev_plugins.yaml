---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: plugins.base.kaja.dev
spec:
  group: base.kaja.dev
  names:
    kind: Plugin
    listKind: PluginList
    plural: plugins
    singular: plugin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.statusEmoji
      name: Status
      type: string
    - jsonPath: .status.checksCompleted
      name: Checks
      type: string
    - jsonPath: .spec.chart
      name: Chart
      type: string
    - jsonPath: .metadata.annotations['kaja\.dev/scope']
      name: Scope
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Plugin is the Schema for the plugins API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Plugin
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations for additional metadata to be applied to
                  the HelmApp
                type: object
              chart:
                description: Chart specifies the Helm chart name or path
                type: string
              clusterRef:
                description: ClusterRef references the cluster where this resource
                  is deployed
                type: string
              createNamespace:
                description: CreateNamespace indicates whether to create the namespace
                type: boolean
              description:
                description: Description provides additional context about the resource
                type: string
              displayName:
                description: DisplayName is a human-readable name for the resource
                type: string
              environmentRef:
                description: EnvironmentRef references the environment this resource
                  belongs to
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels for additional metadata to be applied to the HelmApp
                type: object
              namespace:
                description: Namespace where the release will be installed (overrides
                  default namespace logic)
                type: string
              releaseName:
                description: ReleaseName is the name of the Helm release
                type: string
              repository:
                description: Repository specifies the Helm repository URL
                type: string
              values:
                additionalProperties:
                  type: string
                description: Values contains the values to pass to the chart as key-value
                  pairs
                type: object
              valuesFrom:
                description: ValuesFrom references external sources for values
                items:
                  description: ValuesFromSource represents a source for values
                  properties:
                    configMapKeyRef:
                      description: ConfigMapKeyRef references a key in a ConfigMap
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    secretKeyRef:
                      description: SecretKeyRef references a key in a Secret
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  type: object
                type: array
              version:
                description: Version specifies the chart version
                type: string
            required:
            - chart
            type: object
          status:
            description: status defines the observed state of Plugin
            properties:
              checksCompleted:
                type: string
              message:
                type: string
              pods:
                description: Pods holds basic pod details for display (e.g. ContainerApp
                  overview). Optional.
                items:
                  description: |-
                    PodInfo holds basic pod details for display (e.g. on ContainerApp/HelmApp overview).
                    Used when a resource has associated pods (e.g. ContainerApp's deployment, Helm release pods).
                    Reason and Message surface container waiting/terminated state for debugging (e.g. ImagePullBackOff).
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    ready:
                      type: string
                    reason:
                      type: string
                    restartCount:
                      format: int32
                      type: integer
                  required:
                  - name
                  - phase
                  - ready
                  - restartCount
                  type: object
                type: array
              status:
                type: string
              statusEmoji:
                type: string
              steps:
                items:
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                  required:
                  - message
                  - name
                  - status
                  type: object
                type: array
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
