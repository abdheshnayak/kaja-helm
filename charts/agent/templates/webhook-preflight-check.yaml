{{- if and (or .Values.webhook.enabled .Values.webhook.webhookOnly) (not (lookup "apiextensions.k8s.io/v1" "CustomResourceDefinition" "" "certificates.cert-manager.io")) }}
{{- fail "ERROR: Webhooks are enabled but cert-manager is not installed. Please install cert-manager first:\n  kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.13.3/cert-manager.yaml\n\nOr disable webhooks by setting webhook.enabled=false" }}
{{- end }}
