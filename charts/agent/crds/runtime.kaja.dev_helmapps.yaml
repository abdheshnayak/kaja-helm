---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: helmapps.runtime.kaja.dev
spec:
  group: runtime.kaja.dev
  names:
    kind: HelmApp
    listKind: HelmAppList
    plural: helmapps
    singular: helmapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.statusEmoji
      name: Status
      type: string
    - jsonPath: .status.checksCompleted
      name: Checks
      type: string
    - jsonPath: .spec.chart
      name: Chart
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: HelmApp is the Schema for the helmapps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HelmApp
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations for additional metadata
                type: object
              chart:
                description: Chart specifies the Helm chart name or path
                type: string
              clusterRef:
                description: ClusterRef references the cluster where this resource
                  is deployed
                type: string
              createNamespace:
                description: CreateNamespace indicates whether to create the namespace
                type: boolean
              description:
                description: Description provides additional context about the resource
                type: string
              displayName:
                description: DisplayName is a human-readable name for the resource
                type: string
              domain:
                description: Domain configures a custom or generated domain for the
                  app
                properties:
                  custom:
                    description: Custom indicates if this is a user-provided custom
                      domain or auto-generated
                    type: boolean
                  name:
                    description: Name of the domain (e.g. app.domain.com)
                    type: string
                required:
                - name
                type: object
              environmentRef:
                description: EnvironmentRef references the environment this resource
                  belongs to
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels for additional metadata
                type: object
              namespace:
                description: Namespace where the release will be installed
                type: string
              releaseName:
                description: ReleaseName is the name of the Helm release
                type: string
              repository:
                description: Repository specifies the Helm repository URL
                type: string
              values:
                additionalProperties:
                  type: string
                description: Values contains the values to pass to the chart
                type: object
              valuesFrom:
                description: ValuesFrom references external sources for values
                items:
                  description: ValuesFromSource represents a source for values
                  properties:
                    configMapKeyRef:
                      description: ConfigMapKeyRef references a key in a ConfigMap
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    secretKeyRef:
                      description: SecretKeyRef references a key in a Secret
                      properties:
                        key:
                          type: string
                        name:
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  type: object
                type: array
              valuesYaml:
                description: ValuesYaml contains the raw YAML string of values to
                  pass to the chart
                type: string
              version:
                description: Version specifies the chart version
                type: string
            required:
            - chart
            type: object
          status:
            description: status defines the observed state of HelmApp
            properties:
              checksCompleted:
                type: string
              message:
                type: string
              pods:
                description: Pods holds basic pod details for display (e.g. ContainerApp
                  overview). Optional.
                items:
                  description: |-
                    PodInfo holds basic pod details for display (e.g. on ContainerApp/HelmApp overview).
                    Used when a resource has associated pods (e.g. ContainerApp's deployment, Helm release pods).
                    Reason and Message surface container waiting/terminated state for debugging (e.g. ImagePullBackOff).
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    ready:
                      type: string
                    reason:
                      type: string
                    restartCount:
                      format: int32
                      type: integer
                  required:
                  - name
                  - phase
                  - ready
                  - restartCount
                  type: object
                type: array
              status:
                type: string
              statusEmoji:
                type: string
              steps:
                items:
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                  required:
                  - message
                  - name
                  - status
                  type: object
                type: array
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
