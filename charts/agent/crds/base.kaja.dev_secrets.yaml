---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: secrets.base.kaja.dev
spec:
  group: base.kaja.dev
  names:
    kind: Secret
    listKind: SecretList
    plural: secrets
    singular: secret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.statusEmoji
      name: Status
      type: string
    - jsonPath: .status.checksCompleted
      name: Checks
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Secret is the Schema for the secrets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Secret
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations for additional metadata
                type: object
              basicAuth:
                description: BasicAuth specifies username/password for kubernetes.io/basic-auth
                  secret
                properties:
                  password:
                    type: string
                  username:
                    type: string
                required:
                - password
                - username
                type: object
              clusterRef:
                description: ClusterRef references the cluster where this resource
                  is deployed
                type: string
              data:
                additionalProperties:
                  type: string
                description: Data contains the secret key-value pairs (base64-encoded
                  strings)
                type: object
              description:
                description: Description provides additional context about the resource
                type: string
              displayName:
                description: DisplayName is a human-readable name for the resource
                type: string
              dockerconfigjson:
                description: |-
                  DockerConfigJSON specifies registry credentials to produce a
                  kubernetes.io/dockerconfigjson secret with key .dockerconfigjson
                properties:
                  auths:
                    items:
                      description: DockerRegistryCredential represents a single registry
                        credential
                      properties:
                        email:
                          description: Email is optional in docker config
                          type: string
                        password:
                          description: Password for the registry
                          type: string
                        server:
                          description: Server registry (e.g., https://index.docker.io/v1/
                            or ghcr.io)
                          type: string
                        username:
                          description: Username for the registry
                          type: string
                      required:
                      - server
                      type: object
                    type: array
                type: object
              environmentRef:
                description: EnvironmentRef references the environment this resource
                  belongs to
                type: string
              immutable:
                description: Immutable indicates whether the secret can be modified
                type: boolean
              labels:
                additionalProperties:
                  type: string
                description: Labels for additional metadata
                type: object
              sshAuth:
                description: SSHAuth specifies private key (and optional known_hosts)
                  for kubernetes.io/ssh-auth
                properties:
                  knownHosts:
                    description: KnownHosts is optional known_hosts content
                    type: string
                  privateKey:
                    description: PrivateKey is the PEM-encoded private key
                    type: string
                required:
                - privateKey
                type: object
              stringData:
                additionalProperties:
                  type: string
                description: |-
                  StringData is a write-only, plain-text map mirroring core Secret .stringData
                  Controller will move entries to .data (base64-encoded) and clear this field
                type: object
              tls:
                description: TLS specifies certificate and key to produce a kubernetes.io/tls
                  secret
                properties:
                  cert:
                    description: Cert is the PEM-encoded certificate
                    type: string
                  key:
                    description: Key is the PEM-encoded private key
                    type: string
                required:
                - cert
                - key
                type: object
              type:
                description: Type specifies the type of secret (Opaque, kubernetes.io/tls,
                  etc.)
                type: string
            type: object
          status:
            description: status defines the observed state of Secret
            properties:
              checksCompleted:
                type: string
              message:
                type: string
              pods:
                description: Pods holds basic pod details for display (e.g. ContainerApp
                  overview). Optional.
                items:
                  description: |-
                    PodInfo holds basic pod details for display (e.g. on ContainerApp/HelmApp overview).
                    Used when a resource has associated pods (e.g. ContainerApp's deployment, Helm release pods).
                    Reason and Message surface container waiting/terminated state for debugging (e.g. ImagePullBackOff).
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    ready:
                      type: string
                    reason:
                      type: string
                    restartCount:
                      format: int32
                      type: integer
                  required:
                  - name
                  - phase
                  - ready
                  - restartCount
                  type: object
                type: array
              status:
                type: string
              statusEmoji:
                type: string
              steps:
                items:
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                  required:
                  - message
                  - name
                  - status
                  type: object
                type: array
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
